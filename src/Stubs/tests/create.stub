it('create new :modelToLower:', function () {
    actingAs($this->user);

    $test = Livewire::test(:livewire:::class);

    :vars:

    :sets:

    :newFiles:

    $test->set('createAction', true)
        ->call('save')
        ->assertHasNoErrors();

    :assertDatabase:

    :assertExists:
});