

/*
it('has show :table: page', function () {
    $model = :model:::factory()->create();
    :actingAs:
    test()->get("/$model->path()")->assertStatus(200);
});

it('has edit :table: page', function () {
    $model = :model:::factory()->create();
    :actingAs:
    test()->get("/$model->path()/edit")->assertStatus(200);
});

it('can show an single row from :table:', function () {

    $model = :model:::factory()->create();
    :actingAs:

    livewire(:livewire:)
        ->call('show', $model->id)
        ->assertSee($model->:field:)
        ->assertHasNoErrors();
});

it('has create :table: page', function () {
   :actingAs:
   test()->get('/:uri:/create')->assertStatus(200);
});
*/
